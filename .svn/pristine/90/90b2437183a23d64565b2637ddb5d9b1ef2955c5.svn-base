$(function(){
    
    //获取地址栏参数
    var address = {
        getaddress:function(){
            var search = location.search;
            //解码
            search = decodeURI(search);
            // console.log(search);

            var arr = search.slice(1)
            console.log(arr)

            



        }
    }


address.getaddress();














})